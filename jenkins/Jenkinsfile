pipeline {

    agent any

    node {
      checkout scm
      def cellImage = docker.build("cell-image", "./app/Dockerfile)
    }
    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
