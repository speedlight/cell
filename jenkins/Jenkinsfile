#!/usr/bin/env groovy|

pipeline {

    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
		checkout scm
		def cellImage = docker.build("cell-Image", "./app/Dockerfile")
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
